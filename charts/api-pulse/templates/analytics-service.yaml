{{- $root := . -}}
{{- range $tag := .Values.versionsActive.analytics }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "api-pulse.versionedName" (dict "service" "analytics" "tag" $tag) }}
  labels:
    app.kubernetes.io/name: {{ include "api-pulse.name" $root }}
    app: analytics-service
    version: {{ $tag | quote }}
spec:
  replicas: {{ $root.Values.replicaCount.analytics }}
  selector:
    matchLabels:
      app: analytics-service
      version: {{ $tag | quote }}
  template:
    metadata:
      labels:
        app: analytics-service
        version: {{ $tag | quote }}
    spec:
      {{- include "api-pulse.imagePullSecrets" $root | nindent 6 }}
      containers:
        - name: analytics-service
          image: {{ include "api-pulse.image" (dict "root" $root "repository" $root.Values.images.analytics.repository "tag" $tag) }}
          imagePullPolicy: {{ $root.Values.imagePullPolicy }}
          ports:
            - containerPort: 4002
          env:
            - name: PORT
              value: "4002"
            - name: ANALYTICS_VERSION
              value: {{ $tag | quote }}
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: api-pulse-secrets
                  key: JWT_SECRET
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: api-pulse-config
                  key: DB_HOST
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: api-pulse-config
                  key: DB_PORT
            - name: DB_USER
              valueFrom:
                configMapKeyRef:
                  name: api-pulse-config
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: api-pulse-secrets
                  key: DB_PASSWORD
            - name: REGISTRY_DB
              valueFrom:
                configMapKeyRef:
                  name: api-pulse-config
                  key: REGISTRY_DB
          readinessProbe:
            httpGet:
              path: /health
              port: 4002
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-pulse.versionedName" (dict "service" "analytics" "tag" $tag) }}
  labels:
    app: analytics-service
    version: {{ $tag | quote }}
spec:
  selector:
    app: analytics-service
    version: {{ $tag | quote }}
  ports:
    - port: {{ $root.Values.service.analytics.port }}
      targetPort: {{ $root.Values.service.analytics.targetPort }}
      name: http
{{- end }}
